<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application><activity
            android:exported="true"
            android:name=".MainActivity"
            android:label="@string/create_mobile_geodatabase_app_name">
            
        </activity>
        <provider
            android:name="com.esri.arcgismaps.sample.createmobilegeodatabase.CreateMobileGeodatabaseFileProvider"
            android:authorities="@string/create_mobile_geodatabase_provider_authority"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/create_mobile_geodatabase_provider"/>
        </provider>
    </application>

</manifest>
